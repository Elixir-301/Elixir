public class Triangle extends Shape {
    int base;
    int height;

    public Triangle(int x, int y, string colour, int base, int height) {
        super(x, y, colour);
        this.base = base;
        this.height = height;
    }
}
