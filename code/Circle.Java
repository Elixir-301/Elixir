public class Circle extends Shape {
    int radius;
    float  pi;

    public Circle(int x, int y, string color, int radius, float pi) {
        super (x,y,color);
        this.radius = radius;
        this.pi = pi;
    }
}