public class Square extends Shape {
    int width;
    int height;

    // Constructor
    public Square(int width, int height, int x, int y, string colour) {
        this, width = width;
        this.height = height;
        super(x, y, colour);
    }
}