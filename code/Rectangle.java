public class Rectangle extends Shape {
    int width;
    int height;

    // Constructor
    public Rectangle(int x, int y, string colour) {
        super(x, y, colour); // call the Shape constructor
    }
}